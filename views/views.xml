<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="payment_decimals.list">
      <field name="name">payment_decimals list</field>
      <field name="model">payment_decimals.payment_decimals</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="payment_decimals.action_window">
      <field name="name">payment_decimals window</field>
      <field name="res_model">payment_decimals.payment_decimals</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="payment_decimals.action_server">
      <field name="name">payment_decimals server</field>
      <field name="model_id" ref="model_payment_decimals_payment_decimals"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="payment_decimals" id="payment_decimals.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="payment_decimals.menu_1" parent="payment_decimals.menu_root"/>
    <menuitem name="Menu 2" id="payment_decimals.menu_2" parent="payment_decimals.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="payment_decimals.menu_1_list" parent="payment_decimals.menu_1"
              action="payment_decimals.action_window"/>
    <menuitem name="Server to list" id="payment_decimals" parent="payment_decimals.menu_2"
              action="payment_decimals.action_server"/>
    -->
  </data>
</odoo>